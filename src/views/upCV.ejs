<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upload CV</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .card {
      border-radius: 10px;
      margin-bottom: 20px;
    }
    .upload-container {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
    }
    .upload-box {
      border: 2px dashed #007bff;
      padding: 30px;
      border-radius: 10px;
      width: 100%;
      max-width: 500px;
      text-align: center;
    }
    .upload-box p {
      color: #007bff;
      font-weight: bold;
    }
    .upload-box .form-control {
      border-radius: 10px;
      border: 2px dashed #007bff;
    }
    .btn-upload {
      margin-top: 15px;
    }
    .text-muted {
      font-size: 0.9rem;
    }
    .alert {
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <%- include('partials/header.ejs') %>

  <div class="container my-5">
    <div class="upload-container">
      <div class="card shadow">
        <div class="card-header bg-primary text-white">
          <i class="fa-solid fa-upload me-2"></i> Upload CV
        </div>
        <div class="card-body">
          <div class="upload-box">
            <h4 class="text-primary">Bạn đã có sẵn CV của mình, chỉ cần tải lên CV của bạn.</h4>
            <p class="text-muted">Tiết kiệm thời gian, tìm việc thông minh, nắm bắt cơ hội và làm chủ đường đưa nghề nghiệp của chính mình.</p>

           

            <form action="/user/upload-cv" method="POST" enctype="multipart/form-data">
              <div class="mb-3">
                <input 
                  type="file" 
                  name="cv" 
                  class="form-control" 
                  accept=".pdf,.docx,.doc" 
                  required
                >
                <div class="text-muted">Hỗ trợ định dạng .pdf, .docx, .doc (dưới 5MB)</div>
              </div>
              <button type="submit" class="btn btn-success btn-upload">
                <i class="fa-solid fa-cloud-upload-alt me-2"></i> Tải CV lên
              </button>
            </form>

            <div class="mt-3">
              <h6 class="text-primary">Lịch sử CV đã tải lên:</h6>
              <% if (user.CV && user.CV.length > 0) { %>
                <ul class="list-group">
                  <% user.CV.forEach(function(cv) { %>
                    <li class="list-group-item">
                      <a href="<%= cv.path %>" target="_blank" class="text-decoration-none">
                        <i class="fa-solid fa-file-pdf text-danger me-2"></i> <%= cv.title %>
                      </a>
                    </li>
                  <% }); %>
                </ul>
              <% } else { %>
                <p class="text-muted">Chưa có CV nào được tải lên.</p>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
