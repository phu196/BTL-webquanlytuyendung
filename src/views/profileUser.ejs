<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Thông tin cá nhân</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .card {
      border-radius: 10px;
      margin-bottom: 20px;
    }
    .profile-header {
      display: flex;
      align-items: center;
      gap: 15px;
    }
    .profile-avatar {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      object-fit: cover;
      border: 2px solid #007bff;
    }
    .section-header {
      font-size: 1.1rem;
      font-weight: bold;
      color: #333;
    }
    .cv-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .cv-item a {
      text-decoration: none;
      color: #007bff;
    }
    .cv-item a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <%- include('partials/header.ejs') %>
  <div class="container my-5">
    <!-- Header Row -->
    <div class="row">
        <div class="col-md-2"></div>
      <!-- Avatar and Full Name -->

      <div class="col-md-3">
        <div class="card shadow">
          <div class="card-body profile-header">
            <img 
              src="<%= user.avatarPath || 'https://via.placeholder.com/100' %>" 
              alt="Ảnh đại diện" 
              class="profile-avatar"
            >
            <div>
              <h5 class="text-primary mb-1"><%= user.fullname %></h5>
              <p class="text-secondary mb-0">Vai trò: <span class="text-success"><%= user.role === 'user' ? 'Người dùng' : user.role ==='admin' ? 'Quản trị viên': 'Công ty' %></span></p>
            </div>
          </div>
        </div>
      </div>

      <!-- Thông tin cá nhân -->
      <div class="col-md-5">
        <div class="card shadow">
          <div class="card-header bg-primary text-white">
            <i class="fa-solid fa-user me-2"></i> Thông tin cá nhân
          </div>
          <div class="card-body">
            <p class="mb-2"><span class="section-header">Tên đăng nhập:</span> <%= user.username %></p>
            <p class="mb-2"><span class="section-header">Email:</span> <%= user.email %></p>
            <p class="mb-2"><span class="section-header">Số điện thoại:</span> <%= user.phoneNumber || 'Chưa cung cấp' %></p>
            <p class="mb-2">
              <span class="section-header">Giới tính:</span> 
              <% if (user.gender === 'male') { %>
                Nam
              <% } else if (user.gender === 'female') { %>
                Nữ
              <% } else { %>
                Khác
              <% } %>
            </p>
            <p class="mb-2">
              <span class="section-header">Địa chỉ:</span> 
              <%= user.address.detail || 'Không có' %>, 
              <%= user.address.ward || 'Không có' %>, 
              <%= user.address.district || 'Không có' %>, 
              <%= user.address.province || 'Không có' %>
            </p>
          </div>
        </div>
      </div>
    </div>

    
    <!-- Update Button -->
    <div class="text-center mt-4">
      <button class="btn btn-primary" onclick="window.history.back()">Quay lại</button>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  
  <%- include('partials/footer.ejs') %>
</body>
</html>
