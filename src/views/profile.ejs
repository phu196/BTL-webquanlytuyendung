<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thông tin cá nhân</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <style>
        /* Tổng thể giao diện */
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f4f4f9;
            color: #2d3436;
            padding-bottom: 30px;
        }

        /* Card Profile */
        .profile-card {
            background-color: #fff;
            border: none;
            border-radius: 12px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-top: 40px;
            transition: transform 0.2s, box-shadow 0.3s;
        }

        .profile-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        }

        /* Header */
        .profile-header {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 20px;
            background-color: #6c5ce7;
            color: #fff;
            border-radius: 12px 12px 0 0;
        }

        .profile-header img {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            border: 3px solid #fff;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
        }

        .profile-header .profile-info {
            flex-grow: 1;
        }

        .profile-header h2 {
            font-size: 1.5rem;
            font-weight: bold;
            margin: 0;
        }

        .profile-header p {
            margin: 5px 0;
            font-size: 0.9rem;
            color: #dfe6e9;
        }

        /* Thông tin cá nhân */
        .info-section {
            margin-top: 20px;
        }

        .info-section h5 {
            font-weight: 700;
            margin-bottom: 15px;
        }

        .info-section p {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
            color: #2d3436;
        }

        .info-section p i {
            font-size: 1.2rem;
            color: #6c5ce7;
            margin-right: 10px;
        }

        /* Button */
        .btn-update {
            background-color: #00cec9;
            color: white;
            font-weight: 500;
        }

        .btn-update:hover {
            background-color: #00b894;
        }

        /* Footer */
        footer {
            background-color: #f4f4f9;
            text-align: center;
            padding: 20px;
            margin-top: 40px;
            font-size: 0.9rem;
            color: #636e72;
        }

        footer a {
            text-decoration: none;
            color: #636e72;
            transition: color 0.3s;
        }

        footer a:hover {
            color: #2d3436;
        }
    </style>
</head>

<body>
    <!-- Header -->
    <%- include('partials/header') %>

    <!-- Profile Section -->
    <div class="container">
        <div class="profile-card">
            <!-- Profile Header -->
            <div class="profile-header">
                <img src="<%= user.avatarPath || '/images/user1.png' %>" alt="Avatar">
                <div class="profile-info">
                    <h2><%= user.fullname %></h2>
                    <p>@<%= user.username || 'username' %></p>
                </div>
            </div>
            <!-- Profile Info -->
            <div class="info-section">
                <h5>Thông tin chi tiết:</h5>
                <p><i class="fas fa-envelope"></i> <strong>Email:</strong> <%= user.email %></p>
                <p><i class="fas fa-venus-mars"></i> <strong>Giới tính:</strong>
                    <% if (user.gender === 'male') { %>
                        Nam
                    <% } else if (user.gender === 'female') { %>
                        Nữ
                    <% } else { %>
                        Chưa cập nhật
                    <% } %>
                </p>
                <p><i class="fas fa-phone-alt"></i> <strong>Số điện thoại:</strong> <%= user.phoneNumber || 'Chưa cập nhật' %></p>
                <h5 class="mt-3">Địa chỉ:</h5>
                <p><i class="fas fa-map-marker-alt"></i> <strong>Thôn:</strong> <%= user.address.detail || 'Chưa cập nhật' %></p>
                <p><i class="fas fa-map-marker-alt"></i> <strong>Xã:</strong> <%= user.address.ward || 'Chưa cập nhật' %></p>
                <p><i class="fas fa-map-marker-alt"></i> <strong>Quận:</strong> <%= user.address.district || 'Chưa cập nhật' %></p>
                <p><i class="fas fa-map-marker-alt"></i> <strong>Thành phố:</strong> <%= user.address.province || 'Chưa cập nhật' %></p>
            </div>
            <a href="/user/update" class="btn btn-update w-100 mt-4">Cập nhật thông tin</a>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <div class="container">
            <p>Copyright © 2024 - Công ty Nhóm 16</p>
            <p>Địa chỉ: TC-312 Đại học Bách khoa Hà Nội, Hai Bà Trưng, Hà Nội</p>
            <a href="#">Điều khoản sử dụng</a> | <a href="#">Chính sách bảo mật</a>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
