<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complete Your Information</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #eef2f7;
            font-family: 'Arial', sans-serif;
        }

        .container {
            max-width: 700px;
            background: #ffffff;
            border-radius: 12px;
            padding: 30px;
            margin: 30px auto;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
        }

        h2 {
            color: #2c3e50;
            margin-bottom: 25px;
            font-weight: bold;
            text-align: center;
        }

        .form-label {
            font-weight: bold;
            color: #34495e;
        }

        input[type="text"],
        input[type="email"],
        input[type="tel"],
        input[type="file"],
        select {
            border-radius: 8px;
            border: 1px solid #ced4da;
            padding: 10px;
            transition: all 0.3s ease-in-out;
        }

        input[type="text"]:focus,
        input[type="email"]:focus,
        input[type="tel"]:focus,
        input[type="file"]:focus,
        select:focus {
            border-color: #007bff;
            box-shadow: 0 0 8px rgba(0, 123, 255, 0.3);
        }

        h4 {
            font-size: 20px;
            margin-top: 30px;
            margin-bottom: 15px;
            color: #2c3e50;
            font-weight: bold;
        }

        button {
            width: 100%;
            background-color: #007bff;
            color: #ffffff;
            border-radius: 8px;
            padding: 12px;
            border: none;
            font-size: 16px;
            font-weight: bold;
            transition: all 0.3s ease;
        }

        button:hover {
            background-color: #0056b3;
            box-shadow: 0 5px 15px rgba(0, 91, 187, 0.3);
        }

        .mb-3 {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
<div class="container mt-5">
    <h2>Complete Your Information</h2>
    <form action="/user/<%= user._id %>/created" method="POST" enctype="multipart/form-data">
        <!-- Avatar -->
        <div class="mb-3">
            <label for="avatar" class="form-label">Upload Your Avatar</label>
            <input type="file" id="avatar" name="avatar" accept="image/*" class="form-control" required>
        </div>

        <!-- Username -->
        <div class="mb-3">
            <label for="username" class="form-label">Username</label>
            <input type="text" class="form-control" id="username" name="username" value="<%= user.username %>" readonly>
        </div>

        <!-- Fullname -->
        <div class="mb-3">
            <label for="fullname" class="form-label">Fullname</label>
            <input type="text" class="form-control" id="fullname" name="fullname" value="<%= user.fullname %>" readonly>
        </div>

         <!-- Gender -->
         <div class="mb-3">
            <label for="gender" class="form-label">Your Gender</label>
            <select class="form-select" id="gender" name="gender">
                <option value="male">Male</option>
                <option value="female">Female</option>
                <option value="other">Other</option>
            </select>
        </div>

        <!-- Email -->
        <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input type="email" class="form-control" id="email" name="email" value="<%= user.email %>" readonly>
        </div>

        <!-- Phone Number -->
        <div class="mb-3">
            <label for="phoneNumber" class="form-label">Phone Number</label>
            <input type="tel" class="form-control" id="phoneNumber" name="phoneNumber" value="<%= user.phoneNumber %>" readonly>
        </div>

        <!-- Address -->
        <h4>Your Address</h4>
        <div class="mb-3">
            <label for="detail" class="form-label">Detail</label>
            <input type="text" class="form-control" id="detail" name="address.detail" placeholder="Enter detailed address" value="<%= user.address?.detail || '' %>">
        </div>
        <div class="mb-3">
            <label for="ward" class="form-label">Ward</label>
            <input type="text" class="form-control" id="ward" name="address.ward" placeholder="Enter ward" value="<%= user.address?.ward || '' %>">
        </div>
        <div class="mb-3">
            <label for="district" class="form-label">District</label>
            <input type="text" class="form-control" id="district" name="address.district" placeholder="Enter district" value="<%= user.address?.district || '' %>">
        </div>
        <div class="mb-3">
            <label for="province" class="form-label">Province</label>
            <input type="text" class="form-control" id="province" name="address.province" placeholder="Enter province" value="<%= user.address?.province || '' %>">
        </div>

        <!-- Submit Button -->
        <button type="submit">Complete</button>
    </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
