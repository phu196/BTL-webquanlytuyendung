<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Cài đặt thông tin cá nhân</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
        <style>
            .container {
                max-width: 1200px;
            }
            .card {
                box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
                border-radius: 10px;
                margin-bottom: 16px;
            }
            .profile-pic {
                border-radius: 50%;
                width: 120px;
                height: 120px;
                object-fit: cover;
            }
            .card.text-center {
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                height: 100%;
            }
            .profile-pic {
                margin: 0 auto;
            }
            .job-card:hover {
                transform: translateY(-5px);
                box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.2);
                transition: 0.3s ease-in-out;
            }
            .btn-outline-danger {
                transition: 0.3s;
            }
            .btn-outline-danger:hover {
                background-color: #ff6b6b;
                color: #fff;
            }
            /* Add shadow and border to the table */
            table.table {
                box-shadow: 0px 6px 10px rgba(0, 0, 0, 0.1);
                border: 1px solid #ddd;
                border-radius: 8px;
                overflow: hidden;
            }
            table.table thead {
                border-bottom: 2px solid #ddd;
            }
        </style>
    </head>
    <body>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        <%- include('../partials/header') %>
        <div class="container">
            <div class="row d-flex" style="margin-top: 16px">
                <!-- Các công việc -->
                <div class="col-md-9">
                    <h3 class="card-title">Các công việc đang ứng tuyển</h3>
                    <p></p>

                    <% if (jobs.length > 0) { %> <% jobs.forEach((job, i) => { %>
                    <table class="table">
                        <thead class="table-dark">
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Vị trí</th>
                                <th scope="col">Công ty</th>
                                <th scope="col">Ngày ứng tuyển</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <th scope="row"><%= i + 1 %></th>
                                <td style="max-width: 350px">
                                    <a href="/job/<%= job._id %>" class="text-decoration-none"><%= job.title %></a>
                                </td>
                                <td><%= job.companyName %></td>
                                <td><%= new Date(job.createdAt).toLocaleDateString('vi-VN') %></td>
                            </tr>
                        </tbody>
                    </table>
                    <% }); %> <% } else { %>
                    <p class="text-center text-muted">Hiện tại không có công việc nào được ứng tuyển.</p>
                    <% } %>
                </div>

                <!-- Thông tin cá nhân -->
                <div class="col-md-3" style="margin-bottom: 16px; height: 100%">
                    <div class="card p-4 text-center">
                        <img
                            src="<%= user.avatarPath || '/images/user1.png' %>"
                            alt="User Avatar"
                            class="profile-pic"
                        />
                        <h5 class="mt-3">Chào bạn,</h5>
                        <h6 class="text-muted"><%= user.fullname %></h6>
                        <a href="/user/update" class="btn btn-primary mt-3">Cập nhật thông tin</a>
                    </div>
                </div>
            </div>
        </div>
        <%- include('../partials/footer') %>
    </body>
</html>
